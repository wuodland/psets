<p id="notice"><%= notice %></p>

<h1>Rides</h1>

<table>
  <thead>
    <tr>
      <th>Carno</th>
      <th>Cartype</th>
      <th>Driverphn</th>
      <th>Destination</th>
      <th>Seats</th>
      <th>Time</th>
      <th>Complete</th>
      <th>Seatsleft</th>
      <th></th>
          </tr>
  </thead>

  <tbody>
    <% @rides.each do |ride| %>
      <tr>
        <td><%= ride.carno %></td>
        <td><%= ride.cartype %></td>
        <td><%= ride.driverphn %></td>
        <td><%= ride.destination %></td>
        <td><%= ride.seats %></td>
        <td><%= ride.time %></td>
        <td><%= ride.complete %></td>
        <td><%= ride.seatsleft %></td>
        <td><%= link_to 'Show', ride %></td>
        
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ride', new_ride_path %>
