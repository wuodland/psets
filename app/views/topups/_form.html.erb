<%= form_with(model: topup, local: true) do |form| %>
  <% if topup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(topup.errors.count, "error") %> prohibited this topup from being saved:</h2>

      <ul>
      <% topup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form_row">
  <%= form.label :customer_id %>
  <%= form.collection_select :customer_id, Customer.all, :id, :name %>
</div>

  <div class="field">
    <%= form.label :balance %>
    <%= form.number_field :balance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
